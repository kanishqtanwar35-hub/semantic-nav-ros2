"""semantic_nav — natural language to Nav2 goals.

The ROS nodes live in `*_node.py`. Everything else in this package is plain
Python and importable without ROS, which is what makes the interesting parts
testable.
"""

__version__ = "1.0.0"
